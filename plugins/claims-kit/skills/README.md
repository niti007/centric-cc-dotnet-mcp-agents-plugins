# skills/ — empty on purpose

This directory is part of the `claims-kit` plugin scaffold and is **deliberately empty**.
Populating it is Exercise 2 (see `EXERCISE-2.md` in the repository root).

Delete this README once you have put real content here.
